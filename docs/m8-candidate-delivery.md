# M8 候选交付说明

本候选沿用产品版本0.36.28，以文件名中的M8时间标识区分产物。它不是正式发布，不自动安装SAP助手、替换实际配置、迁移状态或切换现有服务。

## 已有验收与能力边界

2026-09-10 14:22（UTC+08:00），用户在4853完成M7限定集成验收：构建、20项本地协议/凭证/故障测试、w200/200系统信息及有界单表查询全部通过。真实服务返回orvanta/0.36.28。这是旧运行实例的证据，新候选包须单独完成包启动检查。

| 能力           | 已有证据                                 | 保留限制                                     |
| -------------- | ---------------------------------------- | -------------------------------------------- |
| 系统信息       | w200/200、ECC731，含/不含组件两种输出    | 时区独立原生对照仍未补齐                     |
| 单表查询       | 正常、零行、截断、超限拒绝；最多500行    | 不支持任意SQL、联表或聚合                    |
| 协议与凭证保护 | M7本地20项通过，既有真实详情及双凭证证据 | Mock故障不证明真实SAP事务断连或恢复          |
| 质量检查       | 语法及限定SCI已有真实证据                | 原生ATC延期，SCI补充边界未完整验收           |
| 引用分析       | 实现及失败诊断已有证据                   | RIS环境受阻，未取得真实正例                  |
| 调试           | 预检已有证据                             | 真实attach及完整调试闭环未通过               |
| 日志诊断       | 核心日志与关联已有证据                   | 特殊样本延期，不证明跨实例全量历史或因果关系 |
| Agent接入      | 历史限定客户端证据                       | M7 SDK探针不替代各客户端当前验收             |

## 产物与追溯

- 便携ZIP及其SHA256文件：用于交付；包内BUILD-INFO记录文件哈希、Node归档哈希、Git HEAD及dirty状态。
- 同名`-source.zip`及SHA256文件：保存当时的源码、测试、构建脚本、契约、UI和维护文档。不含依赖目录、运行state、真实连接配置、缓存或Git历史。
- 根工作区`.doc/m8-candidate-<时间>/result.json`：绑定源码快照、候选ZIP、M7证据和当前运行目录的磁盘文件对照。
- HEAD不是当前完整源码基线：工作树有累积未提交修改，使用源码快照和逐文件哈希追溯；不自动提交或签名。
- 哈希相同证明读取时文件字节相同，不证明进程已加载的内存模块相同，也不替代候选包运行测试。

源码复建：解压源码ZIP，在Node24及以上和PowerShell7环境安装锁文件依赖（`npm ci --ignore-scripts`），执行`npm run build`。打包脚本会读取Node24.8.0官方校验和并按锁文件安装生产依赖；需要网络。保留依赖版本不等于完成新的安全或许可证审计，归档时间等因素也使ZIP不保证逐字节重现。

## 人工验收

在开发工作区运行根目录`test-m8-candidate.cmd <候选后缀>`。后缀必须是交付记录中的准确值，不能省略；不自动选取“最新包”。

入口只运行既有包启动、配置中心和校验和拒绝测试。先核对候选ZIP哈希，再解压至独立TEMP目录、使用随机本机端口和隔离state；使用虚构SAP配置，不连接真实SAP。会启动临时进程，结束清理测试目录；不启动浏览器、不安装助手、不更改4853或实际凭证。测试日志保存在根`.doc`，结束回传打印的result.json路径即可。

Passed仅证明这些候选包检查通过。干净Windows环境、真实升级/恢复、跨SAP系统及其他Agent客户端仍需独立验收，不能因新ZIP生成而宣布M8全阶段Passed。

## 切换与恢复

本轮不执行切换。后续获批切换时，先保留旧包与完整state、实际配置和原哈希，再停止共享state的旧实例后启动候选；不得用包内示例覆盖实际配置。未知事务结果不自动重试、不删除锁或凭证。降级前须核实旧解析器对新凭证的兼容性，不能恢复旧state快照而丢失升级后的证据。
