dist/
node_modules/
.env
connections.json
*.log
release/
.cache/
# MCP 事件日志：按技能要求写入服务根，属本地诊断证据，不入库
.logs/
# 交付/传输归档不入库，保留在磁盘
/*.zip
# 本地真实 SAP 连接配置不入库，只保留出厂占位符 packaging/windows/default-connections.json
packaging/windows/connections.local.json
# 手工改动前的本地备份：保留在磁盘以便回溯，但不是可提交的源码，且会挡住「工作树必须干净」的打包闸门
*.bak
# 本地测试启动脚本（test-*.cmd）：只在开发机上跑，保留在磁盘但不入库、不推送
test-*.cmd
